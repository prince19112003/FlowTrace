import { day_name } from './day_name';
import { month_name } from './month_name';
import { menu_calculator } from './menu_calculator';

export const matchCaseLessons: Record<string, any> = {
  day_name,
  month_name,
  menu_calculator,
};

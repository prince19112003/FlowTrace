import { positive_number } from './positive_number';
import { divisible_by_5 } from './divisible_by_5';
import { voting_eligibility } from './voting_eligibility';
import { pass_marks } from './pass_marks';
import { square_root_positive } from './square_root_positive';

export const ifStatementLessons: Record<string, any> = {
  positive_number,
  divisible_by_5,
  voting_eligibility,
  pass_marks,
  square_root_positive,
};

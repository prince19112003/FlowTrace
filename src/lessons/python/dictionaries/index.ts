import { create_dict } from './create_dict';
import { update_dict } from './update_dict';
import { traverse_dict } from './traverse_dict';

export const dictionariesLessons: Record<string, any> = {
  create_dict,
  update_dict,
  traverse_dict,
};
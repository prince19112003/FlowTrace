import { recursive_print_n } from './recursive_print_n';
import { recursive_sum } from './recursive_sum';
import { recursive_factorial } from './recursive_factorial';
import { recursive_fibonacci } from './recursive_fibonacci';
import { recursive_power } from './recursive_power';

export const recursionLessons: Record<string, any> = {
  recursive_print_n,
  recursive_sum,
  recursive_factorial,
  recursive_fibonacci,
  recursive_power,
};
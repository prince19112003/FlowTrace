import { linear_search } from './linear_search';
import { binary_search } from './binary_search';
import { bubble_sort } from './bubble_sort';
import { selection_sort } from './selection_sort';
import { insertion_sort } from './insertion_sort';

export const searchingSortingLessons: Record<string, any> = {
  linear_search,
  binary_search,
  bubble_sort,
  selection_sort,
  insertion_sort,
};